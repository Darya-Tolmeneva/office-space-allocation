apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: flowdesk-test

resources:
  - namespace.yaml
  - ../../base

patches:
  - path: patches-backend.yaml
  - path: ingress-patch.yaml

images:
  - name: office-space-allocation/backend
    newTag: test
